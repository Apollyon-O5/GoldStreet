@model IEnumerable<GoldStreet.HistorialEstadoMesa>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClienteMesa.ClienteMesaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EstadoMesa.NombreEstado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClienteMesa.ClienteMesaID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoMesa.NombreEstado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HistorialEstadoMesaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HistorialEstadoMesaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HistorialEstadoMesaID })
        </td>
    </tr>
}

</table>
